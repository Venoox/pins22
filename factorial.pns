fun main(): int = {
    factorial(5);
};

fun factorial(n: int): int = ({
    product = 1;
    while n > 1 do
        product = product * n;
        n = n - 1;
    end;
    product;
} where
    var product: int;
);